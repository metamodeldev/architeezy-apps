/**
 * Table module initialization.
 *
 * Renders the table view based on loaded model data.
 *
 * @module table/binder
 * @package
 */

import { renderTable } from './ui.js';

/**
 * Initializes table: renders the table.
 */
export function initializeTable() {
  renderTable();
}
