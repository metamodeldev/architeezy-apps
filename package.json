{
  "scripts": {
    "format": "oxfmt",
    "lint": "oxlint && stylelint '**/*.css'",
    "lint:fix": "oxlint --fix; stylelint '**/*.css' --fix"
  },
  "devDependencies": {
    "oxfmt": "^0.42.0",
    "oxlint": "^1.57.0",
    "stylelint": "^17.5.0",
    "stylelint-config-standard": "^40.0.0"
  }
}
